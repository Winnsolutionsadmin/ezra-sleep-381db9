# UPSTREAM SOURCE OF TRUTH
This is a public, unlisted mirror of one generated HTML page. The canonical source and data live in a private repository and auto-load via the operator local registry; deliberately not committed here because this repo is public.
